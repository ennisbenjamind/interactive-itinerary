<h1> Share these credentials with fellow travelers! </h1>
<% @trips.each do |trip| %>
<div>
<h2><%= trip.name %></h2>
<li><%= "Trip ID: #{trip.id}" %></li>
<li><%= "Trip Password: #{trip.password}" %></li>
</div>

<% end %>
